package data

import (
	"testing"
)

func TestDatabaseStorage(t *testing.T) {
	//db, _, err := sqlmock.New()
	//require.NoError(t, err)
	//defer func(db *sql.DB) {
	//	_ = db.Close()
	//}(db)
	//dbStorage, err := NewDatabaseStorage(db, zap.NewNop())
	//require.NoError(t, err)
	//t.Run("Get existing counter", func(t *testing.T) {
	//	testGetExistingCounter(dbStorage, t)
	//})
	//t.Run("Get existing gauge", func(t *testing.T) {
	//	testGetExistingGauge(dbStorage, t)
	//})
	//t.Run("Get non existing value", func(t *testing.T) {
	//	testGetNonExistingValue(dbStorage, t)
	//})
}
